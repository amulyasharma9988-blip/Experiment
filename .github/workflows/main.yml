!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Radha's Creation — Handcrafted with Love</title>
  <meta name="description" content="Radha's Creation — Things built with prosperity, Sold with curiosity. Explore our handcrafted collection and shop premium creations." />
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Manrope:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
  <link rel="stylesheet" href="style.css" />
  <!-- EmailJS SDK -->
  <script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@4/dist/email.min.js"></script>
</head>
<body>
  <!-- ====== NAVBAR ====== -->
  <nav class="navbar" id="navbar">
    <div class="nav-container">
      <a href="#" class="nav-logo">
        <span class="logo-icon">✿</span>
        Radha's Creation
      </a>
      <ul class="nav-links">
        <li><a href="#hero">Home</a></li>
        <li><a href="#products">Products</a></li>
        <li><a href="#about">About</a></li>
        <li><a href="#contact">Contact</a></li>
      </ul>
      <div class="nav-actions">
        <button class="cart-btn" id="cartToggleBtn" aria-label="Open cart">
          <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/>
            <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/>
          </svg>
          <span class="cart-count" id="cartCount">0</span>
        </button>
        <button class="mobile-menu-btn" id="mobileMenuBtn" aria-label="Toggle menu">
          <span></span><span></span><span></span>
        </button>
      </div>
    </div>
  </nav>
  <!-- ====== MOBILE MENU ====== -->
  <div class="mobile-menu" id="mobileMenu">
    <ul class="mobile-nav-links">
      <li><a href="#hero">Home</a></li>
      <li><a href="#products">Products</a></li>
      <li><a href="#about">About</a></li>
      <li><a href="#contact">Contact</a></li>
    </ul>
  </div>
  <!-- ====== HERO SECTION ====== -->
  <section class="hero" id="hero">
    <div class="hero-overlay"></div>
    <img
      src="https://cdn.pixabay.com/photo/2016/03/02/20/13/grocery-1232944_1280.jpg"
      alt="Radha's Creation hero banner"
      class="hero-bg"
    />
    <div class="hero-content">
      <span class="hero-badge">✦ Welcome to our shop</span>
      <h1 class="hero-title">Radha's Creation</h1>
      <p class="hero-tagline">Things built with prosperity, Sold with curiosity</p>
      <a href="#products" class="btn btn-primary hero-cta">
        Shop Now
        <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M7 17l9.2-9.2M17 17V7H7"/>
        </svg>
      </a>
    </div>
    <div class="hero-scroll-indicator">
      <div class="scroll-line"></div>
      <span>Scroll</span>
    </div>
  </section>
  <!-- ====== PRODUCTS SECTION ====== -->
  <section class="products-section" id="products">
    <div class="section-container">
      <div class="section-header">
        <span class="section-badge">Our Collection</span>
        <h2 class="section-title">Handpicked Creations</h2>
        <p class="section-subtitle">Each piece is crafted with love and attention to detail</p>
      </div>
      <!-- Carousel Controls -->
      <div class="carousel-controls">
        <button class="carousel-arrow carousel-prev" id="carouselPrev" aria-label="Previous">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M15 18l-6-6 6-6"/>
          </svg>
        </button>
        <button class="carousel-arrow carousel-next" id="carouselNext" aria-label="Next">
          <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <path d="M9 18l6-6-6-6"/>
          </svg>
        </button>
      </div>
      <!-- Product Carousel -->
      <div class="carousel-wrapper">
        <div class="carousel-track" id="carouselTrack">
          <!-- Product cards will be injected by JS -->
        </div>
      </div>
      <!-- Carousel Dots -->
      <div class="carousel-dots" id="carouselDots"></div>
    </div>
  </section>
  <!-- ====== ABOUT SECTION ====== -->
  <section class="about-section" id="about">
    <div class="section-container">
      <div class="about-grid">
        <div class="about-content">
          <span class="section-badge">About Us</span>
          <h2 class="section-title">Crafted with Passion</h2>
          <p class="about-text">
            At <strong>Radha's Creation</strong>, every product tells a story. We believe in the beauty of handcrafted goods, 
            made with love, care, and a touch of creativity. Our mission is to bring you unique, 
            high-quality creations that add warmth and charm to your everyday life.
          </p>
          <p class="about-text">
            From thoughtfully designed pieces to carefully curated collections, we pour our heart into 
            everything we make. Thank you for supporting small businesses and choosing handmade.
          </p>
          <div class="about-stats">
            <div class="stat">
              <span class="stat-number">500+</span>
              <span class="stat-label">Happy Customers</span>
            </div>
            <div class="stat">
              <span class="stat-number">100+</span>
              <span class="stat-label">Unique Products</span>
            </div>
            <div class="stat">
              <span class="stat-number">5★</span>
              <span class="stat-label">Customer Rating</span>
            </div>
          </div>
        </div>
        <div class="about-visual">
          <div class="about-card">
            <div class="about-card-icon">✿</div>
            <h3>Handmade</h3>
            <p>Every item is crafted by hand with premium materials</p>
          </div>
          <div class="about-card">
            <div class="about-card-icon">♡</div>
            <h3>Made with Love</h3>
            <p>Passion and dedication in every single creation</p>
          </div>
          <div class="about-card">
            <div class="about-card-icon">✦</div>
            <h3>Unique Designs</h3>
            <p>One-of-a-kind pieces you won't find anywhere else</p>
          </div>
        </div>
      </div>
    </div>
  </section>
  <!-- ====== CONTACT SECTION ====== -->
  <section class="contact-section" id="contact">
    <div class="section-container">
      <div class="section-header">
        <span class="section-badge">Get in Touch</span>
        <h2 class="section-title">Contact Us</h2>
        <p class="section-subtitle">Have questions? We'd love to hear from you</p>
      </div>
      <div class="contact-info">
        <div class="contact-card">
          <div class="contact-icon">✉</div>
          <h3>Email</h3>
          <a href="mailto:amulyasharma767@gmail.com">amulyasharma767@gmail.com</a>
        </div>
        <div class="contact-card">
          <div class="contact-icon">📱</div>
          <h3>WhatsApp</h3>
          <a href="https://wa.me/919310023280" target="_blank" rel="noopener">+91 93100 23280</a>
        </div>
        <div class="contact-card">
          <div class="contact-icon">📍</div>
          <h3>Visit Us</h3>
          <p>Radha's Creation Store</p>
        </div>
      </div>
    </div>
  </section>
  <!-- ====== FOOTER ====== -->
  <footer class="footer">
    <div class="section-container">
      <div class="footer-content">
        <div class="footer-brand">
          <span class="logo-icon">✿</span> Radha's Creation
        </div>
        <p class="footer-tagline">Things built with prosperity, Sold with curiosity</p>
        <div class="footer-divider"></div>
        <p class="footer-copy">&copy; 2026 Radha's Creation. All rights reserved.</p>
      </div>
    </div>
  </footer>
  <!-- ====== CART MODAL ====== -->
  <div class="cart-overlay" id="cartOverlay"></div>
  <div class="cart-drawer" id="cartDrawer">
    <div class="cart-header">
      <h2>Your Cart</h2>
      <button class="cart-close" id="cartCloseBtn" aria-label="Close cart">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M18 6L6 18"/><path d="M6 6l12 12"/>
        </svg>
      </button>
    </div>
    <div class="cart-items" id="cartItems">
      <div class="cart-empty" id="cartEmpty">
        <svg width="64" height="64" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1" stroke-linecap="round" stroke-linejoin="round" opacity="0.3">
          <circle cx="9" cy="21" r="1"/><circle cx="20" cy="21" r="1"/>
          <path d="M1 1h4l2.68 13.39a2 2 0 0 0 2 1.61h9.72a2 2 0 0 0 2-1.61L23 6H6"/>
        </svg>
        <p>Your cart is empty</p>
        <span>Add some beautiful creations!</span>
      </div>
    </div>
    <div class="cart-footer" id="cartFooter" style="display:none;">
      <div class="cart-total">
        <span>Total</span>
        <span id="cartTotal">₹0</span>
      </div>
      <button class="btn btn-primary btn-full" id="checkoutBtn">
        Proceed to Order
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M5 12h14"/><path d="M12 5l7 7-7 7"/>
        </svg>
      </button>
    </div>
  </div>
  <!-- ====== ORDER FORM MODAL ====== -->
  <div class="modal-overlay" id="orderOverlay"></div>
  <div class="order-modal" id="orderModal">
    <div class="order-modal-header">
      <h2>Complete Your Order</h2>
      <button class="cart-close" id="orderCloseBtn" aria-label="Close order form">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
          <path d="M18 6L6 18"/><path d="M6 6l12 12"/>
        </svg>
      </button>
    </div>
    <form class="order-form" id="orderForm">
      <div class="form-group">
        <label for="customerName">Full Name <span class="required">*</span></label>
        <input type="text" id="customerName" name="customerName" placeholder="Enter your full name" required />
      </div>
      <div class="form-group">
        <label for="customerPhone">Phone Number <span class="required">*</span></label>
        <input type="tel" id="customerPhone" name="customerPhone" placeholder="Enter your phone number" required />
      </div>
      <div class="form-group">
        <label for="customerAddress">Delivery Address</label>
        <textarea id="customerAddress" name="customerAddress" placeholder="Enter your delivery address (optional)" rows="3"></textarea>
      </div>
      <div class="form-group">
        <label for="customerNote">Additional Notes</label>
        <textarea id="customerNote" name="customerNote" placeholder="Any special instructions? (optional)" rows="2"></textarea>
      </div>
      <div class="order-summary">
        <h3>Order Summary</h3>
        <div id="orderSummaryItems"></div>
        <div class="order-summary-total">
          <strong>Total:</strong>
          <strong id="orderTotal">₹0</strong>
        </div>
      </div>
      <button type="submit" class="btn btn-primary btn-full" id="sendOrderBtn">
        <span id="sendOrderText">Send Order via Email</span>
        <span id="sendOrderLoader" class="btn-loader" style="display:none;"></span>
      </button>
      <p class="form-note">Your order details will be sent to the shop owner for confirmation.</p>
    </form>
  </div>
  <!-- ====== TOAST NOTIFICATION ====== -->
  <div class="toast-container" id="toastContainer"></div>
  <!-- ====== WHATSAPP FLOATING BUTTON ====== -->
  <a href="https://wa.me/919310023280" target="_blank" rel="noopener" class="whatsapp-float" aria-label="Chat on WhatsApp">
    <svg viewBox="0 0 32 32" width="28" height="28" fill="#fff">
      <path d="M16.004 0h-.008C7.174 0 0 7.176 0 16.004c0 3.5 1.132 6.738 3.052 9.372L1.056 31.2l6.076-1.952c2.526 1.704 5.54 2.696 8.872 2.696C24.828 31.944 32 24.768 32 16.004S24.828 0 16.004 0zm9.53 22.608c-.396 1.116-2.316 2.136-3.228 2.22-.82.076-1.852.116-2.992-.188a27.3 27.3 0 01-2.716-.996c-4.78-2.06-7.9-6.888-8.14-7.208-.24-.32-1.96-2.612-1.96-4.984s1.24-3.54 1.68-4.024c.44-.484.96-.604 1.28-.604.32 0 .64.004.92.016.296.012.692-.112 1.084.828.396.948 1.352 3.296 1.468 3.536.12.24.196.52.04.84-.16.32-.24.52-.476.8-.24.28-.504.624-.72.84-.24.24-.488.5-.208.98.28.48 1.244 2.052 2.672 3.324 1.836 1.636 3.384 2.144 3.864 2.384.48.24.76.2 1.04-.12.28-.32 1.2-1.4 1.52-1.88.32-.48.64-.4 1.08-.24.44.16 2.792 1.316 3.272 1.556.48.24.8.36.92.556.12.196.12 1.136-.276 2.252z"/>
    </svg>
  </a>
  <script src="script.js"></script>
</body>
</html>
/* ============================================
   RADHA'S CREATION — STYLE SYSTEM
   Modern Minimal + Premium Commerce
   ============================================ */
/* ---- CSS VARIABLES ---- */
:root {
  /* Colors */
  --color-white: #FFFFFF;
  --color-off-white: #FAFBFC;
  --color-gray-50: #F8F9FA;
  --color-gray-100: #F1F3F5;
  --color-gray-200: #E9ECEF;
  --color-gray-300: #DEE2E6;
  --color-gray-400: #CED4DA;
  --color-gray-500: #ADB5BD;
  --color-gray-600: #868E96;
  --color-gray-700: #495057;
  --color-gray-800: #343A40;
  --color-gray-900: #212529;
  --color-black: #111111;
  /* Fresh Green Accents */
  --color-primary: #2D9C6F;
  --color-primary-light: #3DB87F;
  --color-primary-lighter: #E8F5EE;
  --color-primary-dark: #1E7A54;
  --color-primary-gradient: linear-gradient(135deg, #2D9C6F 0%, #3DB87F 50%, #5CC99B 100%);
  /* Accent */
  --color-accent: #F0C75E;
  --color-danger: #E74C3C;
  /* Shadows */
  --shadow-sm: 0 1px 3px rgba(0,0,0,0.06), 0 1px 2px rgba(0,0,0,0.04);
  --shadow-md: 0 4px 12px rgba(0,0,0,0.08), 0 2px 4px rgba(0,0,0,0.04);
  --shadow-lg: 0 12px 36px rgba(0,0,0,0.1), 0 4px 12px rgba(0,0,0,0.06);
  --shadow-xl: 0 20px 50px rgba(0,0,0,0.12), 0 8px 20px rgba(0,0,0,0.08);
  --shadow-green: 0 8px 24px rgba(45,156,111,0.25);
  /* Typography */
  --font-family: 'Manrope', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
  /* Spacing */
  --nav-height: 72px;
  /* Radius */
  --radius-sm: 8px;
  --radius-md: 12px;
  --radius-lg: 16px;
  --radius-xl: 24px;
  --radius-full: 9999px;
  /* Transitions */
  --transition-fast: 200ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-base: 300ms cubic-bezier(0.4, 0, 0.2, 1);
  --transition-smooth: 400ms cubic-bezier(0.16, 1, 0.3, 1);
}
/* ---- RESET ---- */
*, *::before, *::after {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}
html {
  scroll-behavior: smooth;
  -webkit-text-size-adjust: 100%;
}
body {
  font-family: var(--font-family);
  font-size: 16px;
  line-height: 1.6;
  color: var(--color-gray-800);
  background: var(--color-white);
  -webkit-font-smoothing: antialiased;
  -moz-osx-font-smoothing: grayscale;
  overflow-x: hidden;
}
a {
  text-decoration: none;
  color: inherit;
  transition: color var(--transition-fast);
}
button {
  border: none;
  background: none;
  cursor: pointer;
  font-family: inherit;
  font-size: inherit;
}
img {
  display: block;
  max-width: 100%;
}
ul { list-style: none; }
/* ---- UTILITY ---- */
.section-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 24px;
}
/* ---- BUTTONS ---- */
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 14px 32px;
  border-radius: var(--radius-full);
  font-weight: 600;
  font-size: 15px;
  letter-spacing: 0.01em;
  transition: all var(--transition-base);
  cursor: pointer;
  border: none;
  position: relative;
  overflow: hidden;
}
.btn-primary {
  background: var(--color-primary-gradient);
  color: var(--color-white);
  box-shadow: var(--shadow-green);
}
.btn-primary:hover {
  transform: translateY(-2px);
  box-shadow: 0 12px 32px rgba(45,156,111,0.35);
}
.btn-primary:active {
  transform: translateY(0);
}
.btn-full {
  width: 100%;
}
.btn-outline {
  background: transparent;
  color: var(--color-primary);
  border: 2px solid var(--color-primary);
}
.btn-outline:hover {
  background: var(--color-primary);
  color: var(--color-white);
}
/* ============================================
   NAVBAR
   ============================================ */
.navbar {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  height: var(--nav-height);
  z-index: 1000;
  background: rgba(255,255,255,0.85);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-bottom: 1px solid transparent;
  transition: all var(--transition-base);
}
.navbar.scrolled {
  background: rgba(255,255,255,0.95);
  border-bottom-color: var(--color-gray-200);
  box-shadow: var(--shadow-sm);
}
.nav-container {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 24px;
  height: 100%;
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.nav-logo {
  display: flex;
  align-items: center;
  gap: 8px;
  font-size: 20px;
  font-weight: 800;
  color: var(--color-gray-900);
  letter-spacing: -0.02em;
}
.logo-icon {
  font-size: 24px;
  color: var(--color-primary);
}
.nav-links {
  display: flex;
  align-items: center;
  gap: 36px;
}
.nav-links a {
  font-size: 14px;
  font-weight: 500;
  color: var(--color-gray-600);
  position: relative;
  padding: 4px 0;
}
.nav-links a::after {
  content: '';
  position: absolute;
  bottom: -2px;
  left: 0;
  width: 0;
  height: 2px;
  background: var(--color-primary);
  border-radius: 1px;
  transition: width var(--transition-base);
}
.nav-links a:hover {
  color: var(--color-gray-900);
}
.nav-links a:hover::after {
  width: 100%;
}
.nav-actions {
  display: flex;
  align-items: center;
  gap: 12px;
}
.cart-btn {
  position: relative;
  width: 44px;
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-md);
  color: var(--color-gray-700);
  transition: all var(--transition-fast);
}
.cart-btn:hover {
  background: var(--color-gray-100);
  color: var(--color-gray-900);
}
.cart-count {
  position: absolute;
  top: 4px;
  right: 4px;
  width: 20px;
  height: 20px;
  border-radius: var(--radius-full);
  background: var(--color-primary);
  color: var(--color-white);
  font-size: 11px;
  font-weight: 700;
  display: flex;
  align-items: center;
  justify-content: center;
  transform: scale(0);
  transition: transform var(--transition-smooth);
}
.cart-count.visible {
  transform: scale(1);
}
/* Mobile Menu Button */
.mobile-menu-btn {
  display: none;
  flex-direction: column;
  gap: 5px;
  width: 44px;
  height: 44px;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-md);
}
.mobile-menu-btn span {
  width: 22px;
  height: 2px;
  background: var(--color-gray-700);
  border-radius: 2px;
  transition: all var(--transition-base);
}
.mobile-menu-btn.active span:nth-child(1) {
  transform: rotate(45deg) translate(5px, 5px);
}
.mobile-menu-btn.active span:nth-child(2) {
  opacity: 0;
}
.mobile-menu-btn.active span:nth-child(3) {
  transform: rotate(-45deg) translate(5px, -5px);
}
/* Mobile Menu */
.mobile-menu {
  display: none;
  position: fixed;
  top: var(--nav-height);
  left: 0;
  right: 0;
  background: rgba(255,255,255,0.98);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border-bottom: 1px solid var(--color-gray-200);
  padding: 20px 24px;
  z-index: 999;
  transform: translateY(-10px);
  opacity: 0;
  transition: all var(--transition-base);
  pointer-events: none;
}
.mobile-menu.open {
  transform: translateY(0);
  opacity: 1;
  pointer-events: auto;
}
.mobile-nav-links {
  display: flex;
  flex-direction: column;
  gap: 4px;
}
.mobile-nav-links a {
  display: block;
  padding: 14px 16px;
  font-size: 16px;
  font-weight: 500;
  color: var(--color-gray-700);
  border-radius: var(--radius-md);
  transition: all var(--transition-fast);
}
.mobile-nav-links a:hover {
  background: var(--color-gray-100);
  color: var(--color-gray-900);
}
/* ============================================
   HERO
   ============================================ */
.hero {
  position: relative;
  min-height: 100vh;
  display: flex;
  align-items: center;
  justify-content: center;
  overflow: hidden;
}
.hero-bg {
  position: absolute;
  inset: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  object-position: center;
}
.hero-overlay {
  position: absolute;
  inset: 0;
  background: linear-gradient(
    180deg,
    rgba(0,0,0,0.35) 0%,
    rgba(0,0,0,0.55) 50%,
    rgba(0,0,0,0.7) 100%
  );
  z-index: 1;
}
.hero-content {
  position: relative;
  z-index: 2;
  text-align: center;
  padding: 0 24px;
  max-width: 700px;
  animation: heroFadeIn 1s ease-out;
}
@keyframes heroFadeIn {
  from {
    opacity: 0;
    transform: translateY(30px);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}
.hero-badge {
  display: inline-block;
  padding: 8px 20px;
  background: rgba(255,255,255,0.15);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border: 1px solid rgba(255,255,255,0.2);
  border-radius: var(--radius-full);
  color: var(--color-white);
  font-size: 13px;
  font-weight: 500;
  letter-spacing: 0.05em;
  margin-bottom: 24px;
}
.hero-title {
  font-size: clamp(40px, 8vw, 72px);
  font-weight: 800;
  color: var(--color-white);
  line-height: 1.1;
  letter-spacing: -0.03em;
  margin-bottom: 16px;
}
.hero-tagline {
  font-size: clamp(16px, 2.5vw, 20px);
  color: rgba(255,255,255,0.8);
  font-weight: 400;
  max-width: 500px;
  margin: 0 auto 36px;
  line-height: 1.5;
}
.hero-cta {
  font-size: 16px;
  padding: 16px 40px;
}
.hero-scroll-indicator {
  position: absolute;
  bottom: 32px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 2;
  display: flex;
  flex-direction: column;
  align-items: center;
  gap: 8px;
  color: rgba(255,255,255,0.5);
  font-size: 11px;
  font-weight: 500;
  letter-spacing: 0.15em;
  text-transform: uppercase;
  animation: scrollBounce 2s ease-in-out infinite;
}
.scroll-line {
  width: 1px;
  height: 40px;
  background: linear-gradient(to bottom, rgba(255,255,255,0.5), transparent);
}
@keyframes scrollBounce {
  0%, 100% { transform: translateX(-50%) translateY(0); }
  50% { transform: translateX(-50%) translateY(8px); }
}
/* ============================================
   PRODUCTS SECTION
   ============================================ */
.products-section {
  padding: 100px 0 80px;
  background: var(--color-off-white);
}
.section-header {
  text-align: center;
  margin-bottom: 48px;
}
.section-badge {
  display: inline-block;
  padding: 6px 16px;
  background: var(--color-primary-lighter);
  color: var(--color-primary);
  font-size: 13px;
  font-weight: 600;
  border-radius: var(--radius-full);
  letter-spacing: 0.03em;
  margin-bottom: 16px;
}
.section-title {
  font-size: clamp(28px, 4vw, 40px);
  font-weight: 800;
  color: var(--color-gray-900);
  letter-spacing: -0.02em;
  margin-bottom: 12px;
}
.section-subtitle {
  font-size: 16px;
  color: var(--color-gray-500);
  max-width: 500px;
  margin: 0 auto;
}
.carousel-controls {
  display: flex;
  justify-content: flex-end;
  gap: 8px;
  margin-bottom: 24px;
  padding: 0 24px;
  max-width: 1200px;
  margin-left: auto;
  margin-right: auto;
}
.carousel-arrow {
  width: 44px;
  height: 44px;
  border-radius: var(--radius-full);
  background: var(--color-white);
  border: 1px solid var(--color-gray-200);
  color: var(--color-gray-700);
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all var(--transition-fast);
  box-shadow: var(--shadow-sm);
}
.carousel-arrow:hover {
  background: var(--color-primary);
  color: var(--color-white);
  border-color: var(--color-primary);
  box-shadow: var(--shadow-green);
}
.carousel-wrapper {
  overflow: hidden;
  padding: 8px 0 20px;
}
.carousel-track {
  display: flex;
  gap: 24px;
  padding: 0 24px;
  transition: transform var(--transition-smooth);
  cursor: grab;
  user-select: none;
}
.carousel-track:active {
  cursor: grabbing;
}
/* Product Card */
.product-card {
  flex: 0 0 280px;
  background: var(--color-white);
  border-radius: var(--radius-lg);
  overflow: hidden;
  box-shadow: var(--shadow-sm);
  border: 1px solid var(--color-gray-100);
  transition: all var(--transition-base);
  position: relative;
}
.product-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
  border-color: transparent;
}
.product-image-wrapper {
  position: relative;
  width: 100%;
  padding-top: 110%;
  overflow: hidden;
  background: var(--color-gray-100);
}
.product-image-wrapper img {
  position: absolute;
  top: 0;
  left: 0;
  width: 100%;
  height: 100%;
  object-fit: cover;
  transition: transform var(--transition-smooth);
}
.product-card:hover .product-image-wrapper img {
  transform: scale(1.06);
}
.product-badge {
  position: absolute;
  top: 12px;
  left: 12px;
  padding: 4px 12px;
  background: var(--color-primary);
  color: var(--color-white);
  font-size: 11px;
  font-weight: 700;
  border-radius: var(--radius-full);
  letter-spacing: 0.03em;
  text-transform: uppercase;
  z-index: 1;
}
.product-info {
  padding: 20px;
}
.product-name {
  font-size: 16px;
  font-weight: 700;
  color: var(--color-gray-900);
  margin-bottom: 4px;
  line-height: 1.3;
}
.product-desc {
  font-size: 13px;
  color: var(--color-gray-500);
  margin-bottom: 16px;
  line-height: 1.4;
}
.product-bottom {
  display: flex;
  align-items: center;
  justify-content: space-between;
}
.product-price {
  font-size: 20px;
  font-weight: 800;
  color: var(--color-primary-dark);
}
.add-to-cart-btn {
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 6px;
  padding: 10px 18px;
  background: var(--color-primary-lighter);
  color: var(--color-primary);
  font-size: 13px;
  font-weight: 600;
  border-radius: var(--radius-full);
  transition: all var(--transition-fast);
  border: none;
  cursor: pointer;
}
.add-to-cart-btn:hover {
  background: var(--color-primary);
  color: var(--color-white);
  box-shadow: var(--shadow-green);
}
.add-to-cart-btn.added {
  background: var(--color-primary);
  color: var(--color-white);
}
/* Carousel Dots */
.carousel-dots {
  display: flex;
  justify-content: center;
  gap: 8px;
  margin-top: 32px;
}
.carousel-dot {
  width: 8px;
  height: 8px;
  border-radius: var(--radius-full);
  background: var(--color-gray-300);
  border: none;
  cursor: pointer;
  transition: all var(--transition-fast);
  padding: 0;
}
.carousel-dot.active {
  width: 28px;
  background: var(--color-primary);
}
/* ============================================
   ABOUT SECTION
   ============================================ */
.about-section {
  padding: 100px 0;
  background: var(--color-white);
}
.about-grid {
  display: grid;
  grid-template-columns: 1fr 1fr;
  gap: 60px;
  align-items: center;
}
.about-content .section-badge {
  display: inline-block;
}
.about-content .section-title {
  text-align: left;
  margin-bottom: 20px;
}
.about-text {
  font-size: 15px;
  color: var(--color-gray-600);
  line-height: 1.8;
  margin-bottom: 16px;
}
.about-stats {
  display: flex;
  gap: 32px;
  margin-top: 32px;
}
.stat {
  display: flex;
  flex-direction: column;
}
.stat-number {
  font-size: 28px;
  font-weight: 800;
  color: var(--color-primary);
  letter-spacing: -0.02em;
}
.stat-label {
  font-size: 13px;
  color: var(--color-gray-500);
  font-weight: 500;
}
.about-visual {
  display: flex;
  flex-direction: column;
  gap: 16px;
}
.about-card {
  padding: 28px;
  background: var(--color-off-white);
  border-radius: var(--radius-lg);
  border: 1px solid var(--color-gray-100);
  transition: all var(--transition-base);
}
.about-card:hover {
  transform: translateX(4px);
  border-color: var(--color-primary-lighter);
  box-shadow: var(--shadow-md);
}
.about-card-icon {
  font-size: 28px;
  margin-bottom: 12px;
}
.about-card h3 {
  font-size: 16px;
  font-weight: 700;
  color: var(--color-gray-900);
  margin-bottom: 4px;
}
.about-card p {
  font-size: 14px;
  color: var(--color-gray-500);
  line-height: 1.5;
}
/* ============================================
   CONTACT SECTION
   ============================================ */
.contact-section {
  padding: 100px 0;
  background: var(--color-off-white);
}
.contact-info {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 24px;
  max-width: 800px;
  margin: 0 auto;
}
.contact-card {
  background: var(--color-white);
  border-radius: var(--radius-lg);
  padding: 32px 24px;
  text-align: center;
  border: 1px solid var(--color-gray-100);
  transition: all var(--transition-base);
}
.contact-card:hover {
  transform: translateY(-4px);
  box-shadow: var(--shadow-lg);
  border-color: transparent;
}
.contact-icon {
  font-size: 32px;
  margin-bottom: 16px;
}
.contact-card h3 {
  font-size: 16px;
  font-weight: 700;
  color: var(--color-gray-900);
  margin-bottom: 8px;
}
.contact-card a,
.contact-card p {
  font-size: 14px;
  color: var(--color-gray-500);
  line-height: 1.5;
}
.contact-card a:hover {
  color: var(--color-primary);
}
/* ============================================
   FOOTER
   ============================================ */
.footer {
  padding: 60px 0 32px;
  background: var(--color-gray-900);
}
.footer-content {
  text-align: center;
}
.footer-brand {
  font-size: 22px;
  font-weight: 800;
  color: var(--color-white);
  display: flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  margin-bottom: 8px;
}
.footer-brand .logo-icon {
  color: var(--color-primary-light);
}
.footer-tagline {
  font-size: 14px;
  color: var(--color-gray-500);
  margin-bottom: 24px;
}
.footer-divider {
  width: 60px;
  height: 2px;
  background: var(--color-gray-700);
  margin: 0 auto 24px;
  border-radius: 1px;
}
.footer-copy {
  font-size: 13px;
  color: var(--color-gray-600);
}
/* ============================================
   CART DRAWER
   ============================================ */
.cart-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.4);
  backdrop-filter: blur(4px);
  -webkit-backdrop-filter: blur(4px);
  z-index: 2000;
  opacity: 0;
  pointer-events: none;
  transition: opacity var(--transition-base);
}
.cart-overlay.open {
  opacity: 1;
  pointer-events: auto;
}
.cart-drawer {
  position: fixed;
  top: 0;
  right: 0;
  width: 400px;
  max-width: 100vw;
  height: 100vh;
  background: var(--color-white);
  z-index: 2001;
  display: flex;
  flex-direction: column;
  transform: translateX(100%);
  transition: transform var(--transition-smooth);
  box-shadow: -8px 0 32px rgba(0,0,0,0.1);
}
.cart-drawer.open {
  transform: translateX(0);
}
.cart-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 20px 24px;
  border-bottom: 1px solid var(--color-gray-100);
}
.cart-header h2 {
  font-size: 20px;
  font-weight: 700;
  color: var(--color-gray-900);
}
.cart-close {
  width: 40px;
  height: 40px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-md);
  color: var(--color-gray-500);
  transition: all var(--transition-fast);
}
.cart-close:hover {
  background: var(--color-gray-100);
  color: var(--color-gray-900);
}
.cart-items {
  flex: 1;
  overflow-y: auto;
  padding: 16px 24px;
}
.cart-empty {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  height: 100%;
  gap: 12px;
  text-align: center;
}
.cart-empty p {
  font-size: 16px;
  font-weight: 600;
  color: var(--color-gray-700);
}
.cart-empty span {
  font-size: 14px;
  color: var(--color-gray-400);
}
/* Cart Item */
.cart-item {
  display: flex;
  gap: 16px;
  padding: 16px 0;
  border-bottom: 1px solid var(--color-gray-100);
  animation: cartItemIn 300ms ease-out;
}
@keyframes cartItemIn {
  from {
    opacity: 0;
    transform: translateX(20px);
  }
  to {
    opacity: 1;
    transform: translateX(0);
  }
}
.cart-item-image {
  width: 64px;
  height: 64px;
  border-radius: var(--radius-md);
  overflow: hidden;
  flex-shrink: 0;
  background: var(--color-gray-100);
}
.cart-item-image img {
  width: 100%;
  height: 100%;
  object-fit: cover;
}
.cart-item-details {
  flex: 1;
  display: flex;
  flex-direction: column;
  justify-content: center;
}
.cart-item-name {
  font-size: 14px;
  font-weight: 600;
  color: var(--color-gray-900);
  margin-bottom: 4px;
}
.cart-item-price {
  font-size: 15px;
  font-weight: 700;
  color: var(--color-primary);
}
.cart-item-qty {
  display: flex;
  align-items: center;
  gap: 12px;
  margin-top: 8px;
}
.qty-btn {
  width: 28px;
  height: 28px;
  border-radius: var(--radius-full);
  background: var(--color-gray-100);
  color: var(--color-gray-700);
  font-size: 16px;
  font-weight: 600;
  display: flex;
  align-items: center;
  justify-content: center;
  transition: all var(--transition-fast);
}
.qty-btn:hover {
  background: var(--color-gray-200);
}
.qty-value {
  font-size: 14px;
  font-weight: 600;
  color: var(--color-gray-800);
  min-width: 20px;
  text-align: center;
}
.cart-item-remove {
  align-self: flex-start;
  width: 32px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: var(--radius-md);
  color: var(--color-gray-400);
  font-size: 18px;
  transition: all var(--transition-fast);
  flex-shrink: 0;
}
.cart-item-remove:hover {
  background: #FEE2E2;
  color: var(--color-danger);
}
.cart-footer {
  padding: 20px 24px;
  border-top: 1px solid var(--color-gray-100);
  background: var(--color-off-white);
}
.cart-total {
  display: flex;
  justify-content: space-between;
  align-items: center;
  margin-bottom: 16px;
  font-size: 18px;
  font-weight: 700;
  color: var(--color-gray-900);
}
/* ============================================
   ORDER MODAL
   ============================================ */
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(0,0,0,0.5);
  backdrop-filter: blur(6px);
  -webkit-backdrop-filter: blur(6px);
  z-index: 3000;
  opacity: 0;
  pointer-events: none;
  transition: opacity var(--transition-base);
}
.modal-overlay.open {
  opacity: 1;
  pointer-events: auto;
}
.order-modal {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0.95);
  width: 480px;
  max-width: calc(100vw - 32px);
  max-height: calc(100vh - 48px);
  overflow-y: auto;
  background: var(--color-white);
  border-radius: var(--radius-xl);
  z-index: 3001;
  opacity: 0;
  pointer-events: none;
  transition: all var(--transition-smooth);
  box-shadow: var(--shadow-xl);
}
.order-modal.open {
  opacity: 1;
  pointer-events: auto;
  transform: translate(-50%, -50%) scale(1);
}
.order-modal-header {
  display: flex;
  align-items: center;
  justify-content: space-between;
  padding: 24px 28px 16px;
  border-bottom: 1px solid var(--color-gray-100);
}
.order-modal-header h2 {
  font-size: 20px;
  font-weight: 700;
  color: var(--color-gray-900);
}
.order-form {
  padding: 24px 28px 28px;
}
.form-group {
  margin-bottom: 20px;
}
.form-group label {
  display: block;
  font-size: 13px;
  font-weight: 600;
  color: var(--color-gray-700);
  margin-bottom: 6px;
}
.required {
  color: var(--color-danger);
}
.form-group input,
.form-group textarea {
  width: 100%;
  padding: 12px 16px;
  border: 1.5px solid var(--color-gray-200);
  border-radius: var(--radius-md);
  font-family: var(--font-family);
  font-size: 14px;
  color: var(--color-gray-800);
  background: var(--color-white);
  transition: all var(--transition-fast);
  outline: none;
}
.form-group input:focus,
.form-group textarea:focus {
  border-color: var(--color-primary);
  box-shadow: 0 0 0 3px rgba(45,156,111,0.12);
}
.form-group input::placeholder,
.form-group textarea::placeholder {
  color: var(--color-gray-400);
}
.form-group textarea {
  resize: vertical;
}
.order-summary {
  background: var(--color-off-white);
  border-radius: var(--radius-md);
  padding: 16px;
  margin-bottom: 20px;
  border: 1px solid var(--color-gray-100);
}
.order-summary h3 {
  font-size: 14px;
  font-weight: 700;
  color: var(--color-gray-900);
  margin-bottom: 12px;
}
.order-summary-item {
  display: flex;
  justify-content: space-between;
  padding: 6px 0;
  font-size: 13px;
  color: var(--color-gray-600);
}
.order-summary-item span:last-child {
  font-weight: 600;
  color: var(--color-gray-800);
}
.order-summary-total {
  display: flex;
  justify-content: space-between;
  padding-top: 12px;
  margin-top: 12px;
  border-top: 1px solid var(--color-gray-200);
  font-size: 15px;
  color: var(--color-gray-900);
}
.form-note {
  text-align: center;
  font-size: 12px;
  color: var(--color-gray-400);
  margin-top: 12px;
}
/* Button Loader */
.btn-loader {
  width: 18px;
  height: 18px;
  border: 2px solid rgba(255,255,255,0.3);
  border-top-color: white;
  border-radius: 50%;
  animation: spin 0.6s linear infinite;
}
@keyframes spin {
  to { transform: rotate(360deg); }
}
/* ============================================
   TOAST NOTIFICATION
   ============================================ */
.toast-container {
  position: fixed;
  bottom: 24px;
  left: 50%;
  transform: translateX(-50%);
  z-index: 5000;
  display: flex;
  flex-direction: column;
  gap: 8px;
  align-items: center;
}
.toast {
  padding: 14px 24px;
  background: var(--color-gray-900);
  color: var(--color-white);
  font-size: 14px;
  font-weight: 500;
  border-radius: var(--radius-full);
  box-shadow: var(--shadow-lg);
  animation: toastIn 300ms ease-out, toastOut 300ms ease-in 2700ms forwards;
  white-space: nowrap;
}
.toast.success {
  background: var(--color-primary);
}
.toast.error {
  background: var(--color-danger);
}
@keyframes toastIn {
  from {
    opacity: 0;
    transform: translateY(20px) scale(0.95);
  }
  to {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
}
@keyframes toastOut {
  from {
    opacity: 1;
    transform: translateY(0) scale(1);
  }
  to {
    opacity: 0;
    transform: translateY(-10px) scale(0.95);
  }
}
/* ============================================
   WHATSAPP FLOAT
   ============================================ */
.whatsapp-float {
  position: fixed;
  bottom: 28px;
  right: 28px;
  width: 56px;
  height: 56px;
  background: #25D366;
  border-radius: var(--radius-full);
  display: flex;
  align-items: center;
  justify-content: center;
  box-shadow: 0 4px 16px rgba(37,211,102,0.4);
  z-index: 900;
  transition: all var(--transition-base);
  animation: whatsappPulse 2s ease-in-out infinite;
}
.whatsapp-float:hover {
  transform: scale(1.1);
  box-shadow: 0 6px 24px rgba(37,211,102,0.5);
}
@keyframes whatsappPulse {
  0%, 100% { box-shadow: 0 4px 16px rgba(37,211,102,0.4); }
  50% { box-shadow: 0 4px 24px rgba(37,211,102,0.6), 0 0 0 8px rgba(37,211,102,0.1); }
}
/* ============================================
   RESPONSIVE
   ============================================ */
@media (max-width: 768px) {
  :root {
    --nav-height: 64px;
  }
  .nav-links {
    display: none;
  }
  .mobile-menu-btn {
    display: flex;
  }
  .mobile-menu {
    display: block;
  }
  .hero-content {
    padding: 0 20px;
  }
  .hero-scroll-indicator {
    display: none;
  }
  .products-section {
    padding: 72px 0 60px;
  }
  .carousel-controls {
    padding: 0 16px;
    margin-bottom: 16px;
  }
  .carousel-track {
    padding: 0 16px;
    gap: 16px;
  }
  .product-card {
    flex: 0 0 260px;
  }
  .about-section {
    padding: 72px 0;
  }
  .about-grid {
    grid-template-columns: 1fr;
    gap: 40px;
  }
  .about-content .section-title {
    text-align: center;
  }
  .about-content .section-badge {
    display: block;
    text-align: center;
  }
  .about-text {
    text-align: center;
  }
  .about-stats {
    justify-content: center;
  }
  .contact-section {
    padding: 72px 0;
  }
  .contact-info {
    grid-template-columns: 1fr;
    gap: 16px;
  }
  .cart-drawer {
    width: 100vw;
  }
  .whatsapp-float {
    bottom: 20px;
    right: 20px;
    width: 52px;
    height: 52px;
  }
  .section-container {
    padding: 0 16px;
  }
}
@media (max-width: 480px) {
  .product-card {
    flex: 0 0 240px;
  }
  .hero-cta {
    padding: 14px 32px;
    font-size: 15px;
  }
  .about-stats {
    flex-direction: column;
    align-items: center;
    gap: 20px;
  }
  .stat {
    align-items: center;
  }
  .order-modal {
    border-radius: var(--radius-lg);
  }
  .order-form {
    padding: 20px;
  }
}
/* ---- Scroll to top on small screens fix ---- */
@media (max-width: 768px) {
  html {
    scroll-padding-top: var(--nav-height);
  }
}
/* ============================================
   RADHA'S CREATION — JAVASCRIPT
   Cart, Carousel, EmailJS, Interactions
   ============================================ */
// ---- PRODUCT DATA ----
const PRODUCTS = [
  {
    id: 1,
    name: "Handcrafted Floral Set",
    desc: "Beautiful floral arrangement",
    price: 499,
    image: "https://i.ibb.co/BVN5dJ4q/Whats-App-Image-2026-05-30-at-6-52-28-PM.jpg",
    badge: "New"
  },
  {
    id: 2,
    name: "Elegant Design Piece",
    desc: "Premium handmade creation",
    price: 699,
    image: "https://i.ibb.co/5XNmcRc4/Whats-App-Image-2026-05-30-at-6-52-27-PM.jpg",
    badge: null
  },
  {
    id: 3,
    name: "Artisan Collection",
    desc: "Unique craft masterpiece",
    price: 599,
    image: "https://i.ibb.co/d0Z7NCgB/Whats-App-Image-2026-05-30-at-6-52-27-PM-2.jpg",
    badge: "Popular"
  },
  {
    id: 4,
    name: "Premium Creation",
    desc: "Exclusive limited edition",
    price: 899,
    image: "https://i.ibb.co/tT0bCvzZ/Whats-App-Image-2026-05-30-at-6-52-51-PM.jpg",
    badge: null
  },
  {
    id: 5,
    name: "Special Gift Set",
    desc: "Perfect for gifting",
    price: 749,
    image: "https://i.ibb.co/DfhZvgLk/Whats-App-Image-2026-05-30-at-6-52-51-PM-1.jpg",
    badge: "Gift"
  },
  {
    id: 6,
    name: "Classic Design",
    desc: "Timeless handcrafted beauty",
    price: 549,
    image: "https://i.ibb.co/tM7ZHwtq/Whats-App-Image-2026-05-30-at-6-52-27-PM-1.jpg",
    badge: null
  }
];
// ---- EmailJS CONFIG ----
// User needs to update these with their own EmailJS credentials
const EMAILJS_PUBLIC_KEY = "YOUR_PUBLIC_KEY";   // Replace with your EmailJS public key
const EMAILJS_SERVICE_ID = "YOUR_SERVICE_ID";   // Replace with your EmailJS service ID
const EMAILJS_TEMPLATE_ID = "YOUR_TEMPLATE_ID"; // Replace with your EmailJS template ID
// ---- STATE ----
let cart = [];
let carouselPosition = 0;
// ---- DOM ELEMENTS ----
const $ = (sel) => document.querySelector(sel);
const $$ = (sel) => document.querySelectorAll(sel);
// ---- INIT ----
document.addEventListener("DOMContentLoaded", () => {
  initNavbar();
  initMobileMenu();
  renderProducts();
  initCarousel();
  initCart();
  initOrderForm();
  initEmailJS();
});
// ============================================
// NAVBAR
// ============================================
function initNavbar() {
  const navbar = $("#navbar");
  let lastScroll = 0;
  window.addEventListener("scroll", () => {
    const scrollY = window.scrollY;
    if (scrollY > 50) {
      navbar.classList.add("scrolled");
    } else {
      navbar.classList.remove("scrolled");
    }
    lastScroll = scrollY;
  });
  // Smooth scroll for anchor links
  $$('a[href^="#"]').forEach(link => {
    link.addEventListener("click", (e) => {
      e.preventDefault();
      const target = document.querySelector(link.getAttribute("href"));
      if (target) {
        const offset = parseInt(getComputedStyle(document.documentElement).getPropertyValue('--nav-height')) || 72;
        const top = target.getBoundingClientRect().top + window.scrollY - offset;
        window.scrollTo({ top, behavior: "smooth" });
      }
      // Close mobile menu if open
      closeMobileMenu();
    });
  });
}
// ============================================
// MOBILE MENU
// ============================================
function initMobileMenu() {
  const btn = $("#mobileMenuBtn");
  const menu = $("#mobileMenu");
  btn.addEventListener("click", () => {
    btn.classList.toggle("active");
    menu.classList.toggle("open");
  });
  // Close on link click
  menu.querySelectorAll("a").forEach(link => {
    link.addEventListener("click", closeMobileMenu);
  });
}
function closeMobileMenu() {
  $("#mobileMenuBtn").classList.remove("active");
  $("#mobileMenu").classList.remove("open");
}
// ============================================
// PRODUCT RENDERING
// ============================================
function renderProducts() {
  const track = $("#carouselTrack");
  track.innerHTML = PRODUCTS.map(product => `
    <div class="product-card" data-product-id="${product.id}">
      ${product.badge ? `<span class="product-badge">${product.badge}</span>` : ''}
      <div class="product-image-wrapper">
        <img src="${product.image}" alt="${product.name}" loading="lazy" />
      </div>
      <div class="product-info">
        <h3 class="product-name">${product.name}</h3>
        <p class="product-desc">${product.desc}</p>
        <div class="product-bottom">
          <span class="product-price">₹${product.price}</span>
          <button class="add-to-cart-btn" data-id="${product.id}" aria-label="Add ${product.name} to cart">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <path d="M12 5v14M5 12h14"/>
            </svg>
            Add
          </button>
        </div>
      </div>
    </div>
  `).join('');
  // Add to cart listeners
  track.querySelectorAll(".add-to-cart-btn").forEach(btn => {
    btn.addEventListener("click", (e) => {
      e.stopPropagation();
      const id = parseInt(btn.dataset.id);
      addToCart(id);
      // Button animation
      btn.classList.add("added");
      btn.innerHTML = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><path d="M20 6L9 17l-5-5"/></svg> Added`;
      setTimeout(() => {
        btn.classList.remove("added");
        btn.innerHTML = `<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 5v14M5 12h14"/></svg> Add`;
      }, 1500);
    });
  });
}
// ============================================
// CAROUSEL
// ============================================
function initCarousel() {
  const track = $("#carouselTrack");
  const prevBtn = $("#carouselPrev");
  const nextBtn = $("#carouselNext");
  const dotsContainer = $("#carouselDots");
  const cardWidth = 280;
  const gap = 24;
  let visibleCards = getVisibleCards();
  let maxIndex = Math.max(0, PRODUCTS.length - visibleCards);
  // Create dots
  function createDots() {
    dotsContainer.innerHTML = '';
    const dotCount = maxIndex + 1;
    for (let i = 0; i < dotCount; i++) {
      const dot = document.createElement('button');
      dot.className = `carousel-dot ${i === 0 ? 'active' : ''}`;
      dot.setAttribute('aria-label', `Go to slide ${i + 1}`);
      dot.addEventListener('click', () => goToSlide(i));
      dotsContainer.appendChild(dot);
    }
  }
  function getVisibleCards() {
    const containerWidth = track.parentElement.offsetWidth;
    return Math.floor((containerWidth + gap) / (cardWidth + gap)) || 1;
  }
  function updateCarousel() {
    const offset = carouselPosition * (cardWidth + gap);
    track.style.transform = `translateX(-${offset}px)`;
    // Update dots
    dotsContainer.querySelectorAll('.carousel-dot').forEach((dot, i) => {
      dot.classList.toggle('active', i === carouselPosition);
    });
  }
  function goToSlide(index) {
    carouselPosition = Math.max(0, Math.min(index, maxIndex));
    updateCarousel();
  }
  prevBtn.addEventListener("click", () => {
    carouselPosition = Math.max(0, carouselPosition - 1);
    updateCarousel();
  });
  nextBtn.addEventListener("click", () => {
    carouselPosition = Math.min(maxIndex, carouselPosition + 1);
    updateCarousel();
  });
  // Touch/swipe support
  let startX = 0;
  let isDragging = false;
  let startTranslate = 0;
  track.addEventListener("touchstart", (e) => {
    startX = e.touches[0].clientX;
    isDragging = true;
    startTranslate = carouselPosition * (cardWidth + gap);
    track.style.transition = 'none';
  }, { passive: true });
  track.addEventListener("touchmove", (e) => {
    if (!isDragging) return;
    const diff = startX - e.touches[0].clientX;
    track.style.transform = `translateX(-${startTranslate + diff}px)`;
  }, { passive: true });
  track.addEventListener("touchend", (e) => {
    if (!isDragging) return;
    isDragging = false;
    track.style.transition = '';
    const diff = startX - e.changedTouches[0].clientX;
    if (Math.abs(diff) > 50) {
      if (diff > 0) {
        carouselPosition = Math.min(maxIndex, carouselPosition + 1);
      } else {
        carouselPosition = Math.max(0, carouselPosition - 1);
      }
    }
    updateCarousel();
  });
  // Mouse drag support
  track.addEventListener("mousedown", (e) => {
    startX = e.clientX;
    isDragging = true;
    startTranslate = carouselPosition * (cardWidth + gap);
    track.style.transition = 'none';
    e.preventDefault();
  });
  document.addEventListener("mousemove", (e) => {
    if (!isDragging) return;
    const diff = startX - e.clientX;
    track.style.transform = `translateX(-${startTranslate + diff}px)`;
  });
  document.addEventListener("mouseup", (e) => {
    if (!isDragging) return;
    isDragging = false;
    track.style.transition = '';
    const diff = startX - e.clientX;
    if (Math.abs(diff) > 50) {
      if (diff > 0) {
        carouselPosition = Math.min(maxIndex, carouselPosition + 1);
      } else {
        carouselPosition = Math.max(0, carouselPosition - 1);
      }
    }
    updateCarousel();
  });
  // Auto-slide every 4 seconds
  let autoSlideInterval = setInterval(() => {
    carouselPosition = carouselPosition >= maxIndex ? 0 : carouselPosition + 1;
    updateCarousel();
  }, 4000);
  // Pause on hover
  track.addEventListener("mouseenter", () => clearInterval(autoSlideInterval));
  track.addEventListener("mouseleave", () => {
    autoSlideInterval = setInterval(() => {
      carouselPosition = carouselPosition >= maxIndex ? 0 : carouselPosition + 1;
      updateCarousel();
    }, 4000);
  });
  // Responsive recalculation
  window.addEventListener("resize", () => {
    visibleCards = getVisibleCards();
    maxIndex = Math.max(0, PRODUCTS.length - visibleCards);
    carouselPosition = Math.min(carouselPosition, maxIndex);
    createDots();
    updateCarousel();
  });
  createDots();
  updateCarousel();
}
// ============================================
// CART
// ============================================
function initCart() {
  const toggleBtn = $("#cartToggleBtn");
  const closeBtn = $("#cartCloseBtn");
  const overlay = $("#cartOverlay");
  const checkoutBtn = $("#checkoutBtn");
  toggleBtn.addEventListener("click", openCart);
  closeBtn.addEventListener("click", closeCart);
  overlay.addEventListener("click", closeCart);
  checkoutBtn.addEventListener("click", () => {
    closeCart();
    openOrderModal();
  });
}
function openCart() {
  $("#cartDrawer").classList.add("open");
  $("#cartOverlay").classList.add("open");
  document.body.style.overflow = "hidden";
}
function closeCart() {
  $("#cartDrawer").classList.remove("open");
  $("#cartOverlay").classList.remove("open");
  document.body.style.overflow = "";
}
function addToCart(productId) {
  const product = PRODUCTS.find(p => p.id === productId);
  if (!product) return;
  const existing = cart.find(item => item.id === productId);
  if (existing) {
    existing.qty += 1;
  } else {
    cart.push({ ...product, qty: 1 });
  }
  updateCartUI();
  showToast(`${product.name} added to cart!`);
}
function removeFromCart(productId) {
  cart = cart.filter(item => item.id !== productId);
  updateCartUI();
}
function updateQty(productId, delta) {
  const item = cart.find(i => i.id === productId);
  if (!item) return;
  item.qty += delta;
  if (item.qty <= 0) {
    removeFromCart(productId);
    return;
  }
  updateCartUI();
}
function updateCartUI() {
  const countEl = $("#cartCount");
  const cartItemsEl = $("#cartItems");
  const cartEmptyEl = $("#cartEmpty");
  const cartFooterEl = $("#cartFooter");
  const cartTotalEl = $("#cartTotal");
  const totalItems = cart.reduce((sum, item) => sum + item.qty, 0);
  const totalPrice = cart.reduce((sum, item) => sum + item.price * item.qty, 0);
  // Update count badge
  countEl.textContent = totalItems;
  countEl.classList.toggle("visible", totalItems > 0);
  // Update cart items
  if (cart.length === 0) {
    cartEmptyEl.style.display = "flex";
    cartFooterEl.style.display = "none";
    // Remove all cart-item elements
    cartItemsEl.querySelectorAll(".cart-item").forEach(el => el.remove());
  } else {
    cartEmptyEl.style.display = "none";
    cartFooterEl.style.display = "block";
    cartTotalEl.textContent = `₹${totalPrice}`;
    // Re-render cart items
    const existingItems = cartItemsEl.querySelectorAll(".cart-item");
    existingItems.forEach(el => el.remove());
    cart.forEach(item => {
      const el = document.createElement("div");
      el.className = "cart-item";
      el.innerHTML = `
        <div class="cart-item-image">
          <img src="${item.image}" alt="${item.name}" />
        </div>
        <div class="cart-item-details">
          <span class="cart-item-name">${item.name}</span>
          <span class="cart-item-price">₹${item.price * item.qty}</span>
          <div class="cart-item-qty">
            <button class="qty-btn" data-id="${item.id}" data-action="minus" aria-label="Decrease quantity">−</button>
            <span class="qty-value">${item.qty}</span>
            <button class="qty-btn" data-id="${item.id}" data-action="plus" aria-label="Increase quantity">+</button>
          </div>
        </div>
        <button class="cart-item-remove" data-id="${item.id}" aria-label="Remove ${item.name}">✕</button>
      `;
      cartItemsEl.appendChild(el);
    });
    // Attach listeners
    cartItemsEl.querySelectorAll(".qty-btn").forEach(btn => {
      btn.addEventListener("click", () => {
        const id = parseInt(btn.dataset.id);
        const action = btn.dataset.action;
        updateQty(id, action === "plus" ? 1 : -1);
      });
    });
    cartItemsEl.querySelectorAll(".cart-item-remove").forEach(btn => {
      btn.addEventListener("click", () => {
        const id = parseInt(btn.dataset.id);
        removeFromCart(id);
      });
    });
  }
}
// ============================================
// ORDER FORM
// ============================================
function initOrderForm() {
  const closeBtn = $("#orderCloseBtn");
  const overlay = $("#orderOverlay");
  const form = $("#orderForm");
  closeBtn.addEventListener("click", closeOrderModal);
  overlay.addEventListener("click", closeOrderModal);
  form.addEventListener("submit", handleOrderSubmit);
}
function openOrderModal() {
  const modal = $("#orderModal");
  const overlay = $("#orderOverlay");
  const summaryItems = $("#orderSummaryItems");
  const orderTotal = $("#orderTotal");
  // Populate summary
  const totalPrice = cart.reduce((sum, item) => sum + item.price * item.qty, 0);
  summaryItems.innerHTML = cart.map(item => `
    <div class="order-summary-item">
      <span>${item.name} × ${item.qty}</span>
      <span>₹${item.price * item.qty}</span>
    </div>
  `).join('');
  orderTotal.textContent = `₹${totalPrice}`;
  modal.classList.add("open");
  overlay.classList.add("open");
  document.body.style.overflow = "hidden";
}
function closeOrderModal() {
  $("#orderModal").classList.remove("open");
  $("#orderOverlay").classList.remove("open");
  document.body.style.overflow = "";
}
async function handleOrderSubmit(e) {
  e.preventDefault();
  const name = $("#customerName").value.trim();
  const phone = $("#customerPhone").value.trim();
  const address = $("#customerAddress").value.trim();
  const note = $("#customerNote").value.trim();
  const sendBtn = $("#sendOrderBtn");
  const sendText = $("#sendOrderText");
  const sendLoader = $("#sendOrderLoader");
  if (!name || !phone) {
    showToast("Please fill in all required fields", "error");
    return;
  }
  // Prepare order items text
  const totalPrice = cart.reduce((sum, item) => sum + item.price * item.qty, 0);
  const itemsList = cart.map(item =>
    `• ${item.name} × ${item.qty} = ₹${item.price * item.qty}`
  ).join('\n');
  const orderMessage = `
NEW ORDER FROM RADHA'S CREATION
================================
Customer Name: ${name}
Phone Number: ${phone}
${address ? `Delivery Address: ${address}` : ''}
${note ? `Notes: ${note}` : ''}
ITEMS ORDERED:
${itemsList}
TOTAL: ₹${totalPrice}
================================
  `.trim();
  // Show loading state
  sendBtn.disabled = true;
  sendText.style.display = "none";
  sendLoader.style.display = "inline-block";
  try {
    // Try EmailJS
    if (EMAILJS_PUBLIC_KEY !== "YOUR_PUBLIC_KEY") {
      await emailjs.send(EMAILJS_SERVICE_ID, EMAILJS_TEMPLATE_ID, {
        to_email: "amulyasharma767@gmail.com",
        from_name: name,
        phone_number: phone,
        message: orderMessage,
        order_items: itemsList,
        total_amount: `₹${totalPrice}`,
        customer_address: address || "Not provided",
        customer_note: note || "None"
      });
      showToast("Order sent successfully! 🎉", "success");
    } else {
      // Fallback: open mailto
      const subject = encodeURIComponent(`New Order from ${name} - Radha's Creation`);
      const body = encodeURIComponent(orderMessage);
      window.open(`mailto:amulyasharma767@gmail.com?subject=${subject}&body=${body}`, '_blank');
      showToast("Opening email client to send order...", "success");
    }
    // Clear cart & close modal
    cart = [];
    updateCartUI();
    closeOrderModal();
    $("#orderForm").reset();
  } catch (err) {
    console.error("Email send error:", err);
    showToast("Failed to send order. Please try WhatsApp.", "error");
    // Fallback to WhatsApp
    const waMessage = encodeURIComponent(orderMessage);
    setTimeout(() => {
      window.open(`https://wa.me/919310023280?text=${waMessage}`, '_blank');
    }, 1500);
  } finally {
    sendBtn.disabled = false;
    sendText.style.display = "inline";
    sendLoader.style.display = "none";
  }
}
// ============================================
// EMAILJS INIT
// ============================================
function initEmailJS() {
  if (typeof emailjs !== 'undefined' && EMAILJS_PUBLIC_KEY !== "YOUR_PUBLIC_KEY") {
    emailjs.init(EMAILJS_PUBLIC_KEY);
  }
}
// ============================================
// TOAST
// ============================================
function showToast(message, type = "") {
  const container = $("#toastContainer");
  const toast = document.createElement("div");
  toast.className = `toast ${type}`;
  toast.textContent = message;
  container.appendChild(toast);
  setTimeout(() => {
    if (toast.parentNode) toast.remove();
  }, 3200);
}
// ============================================
// INTERSECTION OBSERVER FOR ANIMATIONS
// ============================================
const observerOptions = {
  threshold: 0.1,
  rootMargin: '0px 0px -50px 0px'
};
const observer = new IntersectionObserver((entries) => {
  entries.forEach(entry => {
    if (entry.isIntersecting) {
      entry.target.style.opacity = '1';
      entry.target.style.transform = 'translateY(0)';
    }
  });
}, observerOptions);
// Observe sections after DOM load
document.addEventListener("DOMContentLoaded", () => {
  const animatedSections = $$('.about-card, .contact-card, .stat');
  animatedSections.forEach(el => {
    el.style.opacity = '0';
    el.style.transform = 'translateY(20px)';
    el.style.transition = 'opacity 0.6s ease-out, transform 0.6s ease-out';
    observer.observe(el);
  });
});
